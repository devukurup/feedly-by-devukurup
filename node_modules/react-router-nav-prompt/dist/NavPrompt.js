import React from 'react';
import useNavPrompt from './useNavPrompt';

const NavPrompt = ({
  shouldBlock,
  children
}) => {
  const config = useNavPrompt({
    shouldBlock
  });
  return children(config);
};

export default NavPrompt;